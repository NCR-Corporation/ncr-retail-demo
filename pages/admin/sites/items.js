const { Table } = require("reactstrap");

function Items({ }) {

  return (
    <Table striped>
      <thead>
        <tr>
          <th>Name</th>
        </tr>
      </thead>
    </Table>
  )
}

export default Items;