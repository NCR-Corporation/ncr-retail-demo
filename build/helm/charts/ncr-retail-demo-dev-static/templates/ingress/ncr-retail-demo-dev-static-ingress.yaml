{{- if .Values.Ingress.RetailDemoDev }}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ $.Values.Master.App.Prefix }}-ingress
  annotations:
    kubernetes.io/ingress.global-static-ip-name: {{ .Values.Ingress.RetailDemoDev.IpName }}
    kubernetes.io/ingress.class: gce
    kubernetes.io/ingress.allow-http: "true"
    networking.gke.io/v1beta1.FrontendConfig: frontend-config-http-to-https-redirect-{{ .Release.Namespace }}
spec:
  {{- if .Values.Ingress.RetailDemoDev.TlsSecret }}
  tls:
    - hosts:
        - {{ .Values.Ingress.RetailDemoDev.Host }}
      secretName: {{ .Values.Ingress.RetailDemoDev.TlsSecret }}
  {{- end}}
  rules:
    - host: {{ .Values.Ingress.RetailDemoDev.Host }}
      http:
        paths:
          - backend:
              serviceName: {{ $.Release.Namespace }}
              servicePort: 80
{{- end}}
