apiVersion: v1
kind: Service
metadata:
  name: {{ $.Release.Name }}-{{ .Values.Master.App.ReleaseVersion }}-ncrrd-frontend
  labels:
    app: {{ .Values.Master.App.Prefix }}-ncrrd-frontend
    version: {{ $.Chart.Version }}
    release: {{ $.Release.Name }}
spec:
  type: NodePort
  selector:
    app: {{ .Values.Master.App.Prefix }}-ncrrd-frontend
    release: {{ $.Release.Name }}
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
status:
  loadBalancer: {}