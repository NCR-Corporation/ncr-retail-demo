apiVersion: v1
kind: Service
metadata:
  name: {{ $.Release.Name }}-{{ .Values.Master.App.ReleaseVersion }}-srd-frontend
  labels:
    app: {{ .Values.Master.App.Prefix }}-srd-frontend
    version: {{ $.Chart.Version }}
    release: {{ $.Release.Name }}
spec:
  type: NodePort
  selector:
    app: {{ .Values.Master.App.Prefix }}-srd-frontend
    release: {{ $.Release.Name }}
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
status:
  loadBalancer: {}