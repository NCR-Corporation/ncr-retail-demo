apiVersion: v1
kind: Service
metadata:
  name: {{ $.Release.Name }}-{{ .Values.Master.App.ReleaseVersion }}-ncr-retail-demo
  labels:
    app: {{ .Values.Master.App.Prefix }}-ncr-retail-demo
    version: {{ $.Chart.Version }}
    release: {{ $.Release.Name }}
spec:
  type: NodePort
  selector:
    app: {{ .Values.Master.App.Prefix }}-ncr-retail-demo
    release: {{ $.Release.Name }}
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
status:
  loadBalancer: {}